# Анализ проблемы PASSWORD_HASH_INVALID в 2FA

## Проблема
При попытке аутентификации с 2FA паролем через `auth.checkPassword` получаем ошибку:
```
400: PASSWORD_HASH_INVALID (auth.checkPassword)
```

## Подтверждено
✅ Пароль **точно верный** - работает в официальном Telegram клиенте
✅ Пароль передается корректно (длина: 8, тип: string)
✅ Сессия 2FA найдена и валидна
✅ SRP параметры получены (g, p, salt1, salt2, srp_B, srp_id)

## Текущая реализация

### Используемые библиотеки
- `@mtkruto/node` v0.77.1 - для MTProto API
- `tssrp6a` v3.0.0 - для SRP вычислений

### Проблема с текущей реализацией
Мы используем **самописную реализацию SRP** в `backend/src/modules/auth/auth.service.ts` (строки 1077-1206):

1. Вычисляем `x` через PBKDF2
2. Генерируем случайное `a`
3. Вычисляем `A = g^a mod p`
4. Вычисляем `u`, `k`, `S` через `tssrp6a`
5. Вычисляем `M1` по формуле: `SHA256((SHA256(p) XOR SHA256(g)) || SHA256(A) || SHA256(B) || K)` где `K = SHA256(S)`

### Возможные причины ошибки

#### 1. Неправильная формула M1
Telegram использует **модифицированный SRP протокол**, отличающийся от стандартного SRP-6a.

Возможные варианты формулы M1:
- Текущая: `M1 = SHA256((SHA256(p) XOR SHA256(g)) || SHA256(A) || SHA256(B) || K)` где `K = SHA256(S)`
- Стандартная SRP: `M1 = H(A || B || S)`
- Другие варианты из документации

#### 2. Неправильное вычисление S
Формула: `S = (B - k * g^x)^(a + u * x) mod p`

Возможные проблемы:
- Неправильное вычисление `k = H(N || g)` или `k = H(p || g)`
- Неправильное вычисление `u = H(A || B)`
- Проблемы с BigInt арифметикой

#### 3. Неправильное вычисление x
Текущая формула:
```typescript
x = PBKDF2(salt1 + password + salt2, salt1, 100000, 256, 'sha256')
```

Возможно, нужна другая формула или другой порядок конкатенации.

#### 4. Проблемы с преобразованием типов
- Buffer ↔ BigInt
- Padding (256 байт для A, B, S)
- Byte order (big-endian vs little-endian)

## Решения

### Решение 1: Использовать готовую библиотеку (РЕКОМЕНДУЕТСЯ)

Установить `gramjs` или `mtcute`, которые имеют **проверенную реализацию** Telegram SRP:

```bash
npm install telegram@latest
# или
npm install @mtcute/node
```

Пример с `gramjs`:
```typescript
import { computeCheck } from 'telegram/Password';

const check = await computeCheck(passwordResult, password);
await client.invoke({
  _: 'auth.checkPassword',
  password: check,
});
```

### Решение 2: Исправить текущую реализацию

Изучить исходный код `gramjs` или `mtcute` и скопировать их реализацию SRP:
- https://github.com/gram-js/gramjs/blob/master/gramjs/Password.ts
- https://github.com/mtcute/mtcute/tree/master/packages/core/src/highlevel/auth

### Решение 3: Использовать официальную документацию

Реализовать SRP строго по официальной документации Telegram:
- https://core.telegram.org/api/srp
- https://core.telegram.org/method/auth.checkPassword

## Рекомендации

### Краткосрочное решение
1. Добавить детальное логирование всех промежуточных значений:
   - `x`, `a`, `A`, `u`, `k`, `S`, `M1`
   - Сравнить с рабочей реализацией

2. Попробовать альтернативные формулы M1

### Долгосрочное решение
**Использовать `gramjs` или `mtcute`** вместо самописной реализации.

Эти библиотеки:
- ✅ Проверены временем
- ✅ Используются в production
- ✅ Поддерживаются сообществом
- ✅ Имеют правильную реализацию MTProto SRP

## Статус

❌ **Текущая реализация не работает**
🔄 **Требуется переписать с использованием проверенной библиотеки**

## Ссылки

- [Telegram SRP Documentation](https://core.telegram.org/api/srp)
- [auth.checkPassword Method](https://core.telegram.org/method/auth.checkPassword)
- [gramjs Password.ts](https://github.com/gram-js/gramjs/blob/master/gramjs/Password.ts)
- [mtcute SRP Implementation](https://github.com/mtcute/mtcute/tree/master/packages/core/src/highlevel/auth)
- [tssrp6a Library](https://www.npmjs.com/package/tssrp6a)

## Временное решение для пользователя

До исправления проблемы рекомендуется:
1. Использовать QR-код авторизацию (если доступна)
2. Временно отключить 2FA для подключения аккаунта
3. Использовать официальный Telegram клиент для управления сессиями

