# План развития функциональности

## Инструкции по запуску

### Запуск через Docker

```bash
docker compose build
docker compose up -d
docker compose logs -f
docker compose down
```

### Локальный запуск

```bash
npm install
npm run dev
```

### Запуск в продакшен на выделенном сервере Ubuntu

```bash
# Настройка на сервере
```

---

## 1. Клиенты и CRM

### Приоритет: Высокий

- [x] История посещений клиента (все записи, услуги, мастера)
  - **Реализовано:** В админ-панели на странице клиента (`admin/app/clients/[id]/page.tsx`) отображается список всех записей клиента с информацией об услугах и мастерах. Также есть страница истории в frontend (`frontend/src/pages/History.tsx`).
  - **Улучшения:** Добавить фильтры по дате, статусу, мастеру. Добавить экспорт истории в PDF/Excel. Добавить график посещений по месяцам.

- [ ] Статистика клиента (частота посещений, средний чек, любимые услуги)
  - **Частично реализовано:** Есть базовая информация о клиенте и его записях.
  - **Улучшения:** Добавить расчет частоты посещений, среднего чека, топ услуг. Добавить визуализацию статистики (графики, диаграммы).

- [x] Система лояльности (бонусы, скидки, промокоды)
  - **Реализовано:** Полностью реализована система бонусов (`backend/src/modules/financial/financial.service.ts`). Есть начисление бонусов за услуги (`bonusPointsEarned`, `bonusPointsUsed` в Appointment), автоматическое начисление после завершения записи (scheduler), использование бонусов при оплате. **Реализованы транзакции** (Transaction entity) с типами: PAYMENT, BONUS_EARNED, REFUND. **Реализован возврат бонусов** при отмене записи (`refundBonusPoints`). **Реализована история транзакций** (`getUserTransactions`) с привязкой к записям. Есть расчет бонусов на основе процента от услуги (`calculateBonusPoints`).
  - **Улучшения:** Добавить промокоды и купоны. Добавить систему уровней лояльности (VIP, Gold, Silver). Добавить настраиваемые правила начисления бонусов. Добавить экспорт истории транзакций.

- [x] Сегментация клиентов (VIP, постоянные, новые, неактивные)
  - **Реализовано:** Добавлены поля `segment` и `tags` в User entity. Реализован автоматический cron-задачник `updateClientSegments` в SchedulerService, который ежедневно обновляет сегментацию клиентов на основе частоты посещений и суммы покупок. Сегменты: VIP (≥10 посещений и ≥10000₽), постоянный (≥5 посещений), новый (0 посещений), неактивный (>90 дней с последнего визита).
  - **Улучшения:** Добавить UI для ручного управления метками и категориями в админ-панели. Добавить фильтрацию клиентов по сегментам. Добавить автоматические действия для разных сегментов (скидки, рассылки).

- [x] Автоматические напоминания о днях рождения
  - **Реализовано:** Добавлено поле `dateOfBirth` в User entity. Реализован cron-задачник `sendBirthdayReminders` в SchedulerService, который ежедневно в 9:00 проверяет дни рождения и отправляет поздравления через Telegram. Учитывается настройка `birthdayRemindersEnabled` в `notificationSettings` клиента.
  - **Улучшения:** Добавить поле даты рождения в форму редактирования клиента в админ-панели. Добавить специальные предложения и скидки для именинников. Добавить настройку времени отправки поздравлений.

- [ ] Черный список клиентов с причиной
  - **Не реализовано:** Нет функционала черного списка.
  - **Улучшения:** Добавить поле `isBlacklisted` и `blacklistReason` в User entity. Добавить фильтр в списке клиентов.

- [x] История взаимодействий (звонки, сообщения, комментарии)
  - **Реализовано:** Есть история взаимодействий (`admin/app/clients/[id]/page.tsx`), отображается через API `/users/${clientId}/interaction-history`. Есть возможность отправки сообщений в Telegram через админ-панель.
  - **Улучшения:** Добавить запись звонков. Добавить комментарии админа к взаимодействиям. Добавить фильтры по типу взаимодействия.

- [x] Тэги и метки для клиентов
  - **Реализовано:** Добавлено поле `tags` (JSONB массив строк) в User entity. Поле доступно для хранения ручных меток и категорий.
  - **Улучшения:** Добавить UI для управления тегами в админ-панели. Добавить предустановленные теги (VIP, постоянный клиент, проблемный и т.д.). Добавить фильтрацию клиентов по тегам. Добавить связь many-to-many с отдельной таблицей тегов для лучшей нормализации.

---

## 2. Записи и расписание

### Приоритет: Высокий

- [ ] Повторяющиеся записи (еженедельно, ежемесячно)
  - **Не реализовано:** Нет функционала повторяющихся записей.
  - **Улучшения:** Добавить поле `recurrencePattern` в Appointment entity. Добавить UI для настройки повторений. Добавить автоматическое создание записей по расписанию.

- [ ] Групповые записи (несколько клиентов одновременно)
  - **Не реализовано:** Нет функционала групповых записей.
  - **Улучшения:** Добавить связь many-to-many между Appointment и User. Добавить возможность выбора нескольких клиентов при создании записи.

- [ ] Ожидание в очереди (если слот занят)
  - **Не реализовано:** Нет системы очереди.
  - **Улучшения:** Добавить таблицу Queue с приоритетами. Добавить уведомления при освобождении слота.

- [ ] Автоматический подбор мастера по загруженности
  - **Не реализовано:** Нет автоматического подбора.
  - **Улучшения:** Использовать существующий `getMasterLoad` из AnalyticsService для подбора наименее загруженного мастера.

- [ ] Интеграция с календарями (Google Calendar, Outlook)
  - **Не реализовано:** Нет интеграций.
  - **Улучшения:** Добавить экспорт в iCal формат. Добавить синхронизацию с Google Calendar API.

- [ ] Экспорт расписания в PDF/Excel
  - **Частично реализовано:** Есть компонент ExportButton, но нет специфичного экспорта расписания.
  - **Улучшения:** Добавить экспорт календаря в PDF/Excel с форматированием. Добавить экспорт по мастерам или датам.

- [x] Статистика загруженности мастеров
  - **Реализовано:** Есть метод `getMasterLoad` в AnalyticsService (`backend/src/modules/analytics/analytics.service.ts`), доступен через API `/analytics/master-load`.
  - **Улучшения:** Добавить визуализацию загруженности в админ-панели. Добавить прогнозирование загруженности.

- [x] Блокировка времени для перерывов/обедов
  - **Реализовано:** Есть сущность BlockInterval (`backend/src/entities/block-interval.entity.ts`), используется при проверке доступности времени в AppointmentsService.
  - **Улучшения:** Добавить UI для управления блокировками в расписании мастера. Добавить автоматические блокировки по расписанию.

- [x] Перенос записей с уведомлением клиента
  - **Реализовано:** Есть метод `reschedule` в AppointmentsService, отправляется уведомление через `sendAppointmentRescheduled`. Также есть drag & drop в календаре админ-панели. **Добавлен функционал переноса через Telegram бот** с выбором причины (изменение планов, болезнь, срочные дела, другая причина с текстовым вводом). Причина сохраняется при переносе записи. **Исправлено отображение записей в календаре** - все записи, включая комбинированные (несколько подкатегорий), корректно отображаются в календаре админ-панели. Улучшена логика фильтрации событий по дням и часам. Исправлена обработка дат для корректного отображения записей в нужных временных слотах.
  - **Улучшения:** Добавить уведомление мастера о переносе записи. Добавить историю переносов в профиле клиента.

---

## 3. Финансы и аналитика

### Приоритет: Высокий

- [x] Детальная финансовая отчетность (доходы, расходы, прибыль)
  - **Реализовано:** Есть базовая аналитика в Dashboard (`admin/app/dashboard/page.tsx`), используется AnalyticsService для получения статистики по доходам, записям, транзакциям. Есть графики доходов по дням. **Реализован AnalyticsService** (`backend/src/modules/analytics/analytics.service.ts`) с методами: `getDashboardStats` (статистика по записям, доходам, мастерам, графикам по дням), `getMasterLoad` (загруженность мастера за период). Есть API эндпоинты `/analytics/dashboard` и `/analytics/master-load`.
  - **Улучшения:** Добавить отчеты по расходам. Добавить расчет прибыли. Добавить детальные финансовые отчеты с группировкой по периодам, услугам, мастерам. Добавить экспорт аналитики в Excel/PDF.

- [ ] Статистика по услугам (популярность, доходность)
  - **Частично реализовано:** Есть базовая информация об услугах в статистике.
  - **Улучшения:** Добавить отдельный раздел статистики по услугам. Добавить расчет популярности и доходности каждой услуги.

- [ ] Статистика по мастерам (заработок, количество клиентов)
  - **Частично реализовано:** Есть статистика загруженности мастеров.
  - **Улучшения:** Добавить расчет заработка каждого мастера. Добавить статистику по количеству клиентов. Добавить рейтинг мастеров.

- [ ] Прогнозирование доходов
  - **Не реализовано:** Нет прогнозирования.
  - **Улучшения:** Использовать исторические данные для прогнозирования. Добавить ML модели для более точных прогнозов.

- [ ] Экспорт финансовых отчетов
  - **Частично реализовано:** Есть компонент ExportButton, но нет специфичного экспорта финансов.
  - **Улучшения:** Добавить экспорт финансовых отчетов в Excel/PDF с детализацией. Добавить различные форматы отчетов.

- [ ] Интеграция с бухгалтерией (1С, МойОфис)
  - **Не реализовано:** Нет интеграций.
  - **Улучшения:** Добавить экспорт данных в форматы 1С. Добавить API для интеграции.

- [ ] Система скидок и акций
  - **Не реализовано:** Нет системы скидок.
  - **Улучшения:** Добавить таблицу Discounts/Promotions. Добавить применение скидок при создании записи.

- [ ] Управление ценами (сезонные изменения, скидки)
  - **Не реализовано:** Цены фиксированы в Service entity.
  - **Улучшения:** Добавить историю изменения цен. Добавить сезонные цены. Добавить динамическое ценообразование.

---

## 4. Уведомления и коммуникации

### Приоритет: Средний

- [x] Настройки системы (timezone, интервалы напоминаний, настройки бронирования)
  - **Реализовано:** Полностью реализован SettingsModule (`backend/src/modules/settings/`) с SettingsService и SettingsController. **Реализованы настройки timezone** - используется для корректного отображения времени в уведомлениях и боте. **Реализованы настройки интервалов напоминаний** (`reminderIntervals`) - глобальные настройки с возможностью обновления через API `/settings/reminder-intervals` (GET/PUT). **Реализованы настройки бронирования** (`bookingSettings`) - manualConfirmation, minAdvanceBooking, maxAdvanceBooking, cancellationDeadline. **Реализованы настройки скидки для первого визита** (`firstVisitDiscount`). Все изменения логируются через AuditService.
  - **Улучшения:** Добавить UI для всех настроек в админ-панели. Добавить валидацию настроек. Добавить историю изменений настроек. Добавить экспорт/импорт настроек.

- [ ] Email-уведомления (в дополнение к Telegram)
  - **Частично реализовано:** Есть enum NotificationChannel.EMAIL и структура для email, но только заглушка (console.log) в NotificationsService.
  - **Улучшения:** Интегрировать email сервис (Nodemailer, SendGrid). Добавить настройки SMTP. Реализовать отправку email уведомлений.

- [ ] SMS-уведомления через SMS-шлюз
  - **Частично реализовано:** Есть enum NotificationChannel.SMS и структура для SMS, но только заглушка (console.log) в NotificationsService.
  - **Улучшения:** Интегрировать SMS шлюз (SMS.ru, Twilio). Добавить настройки SMS провайдера. Реализовать отправку SMS уведомлений.

- [ ] Push-уведомления в мобильном приложении
  - **Не реализовано:** Нет мобильного приложения.
  - **Улучшения:** После создания мобильного приложения добавить push уведомления через Firebase Cloud Messaging или аналоги.

- [x] Шаблоны сообщений для разных ситуаций
  - **Реализовано:** Есть сущность Template (`backend/src/entities/template.entity.ts`), используется в NotificationsService для шаблонов уведомлений. Есть дефолтные шаблоны для разных типов уведомлений. **Добавлен полный UI для управления шаблонами** (`admin/app/templates/page.tsx`): создание, редактирование, удаление шаблонов. **Создан модуль Templates** (`backend/src/modules/templates/`) с service, controller и DTO. **Добавлен предпросмотр шаблонов** с тестовыми данными. **Добавлено отображение доступных переменных** для каждого типа уведомления. Поддержка Handlebars-шаблонов с переменными в формате `{{variableName}}`.
  - **Улучшения:** Добавить больше предустановленных шаблонов. Добавить валидацию шаблонов. Добавить историю изменений шаблонов.

- [x] Автоматические отзывы после посещения
  - **Реализовано:** Есть метод `sendFeedbackRequest` в NotificationsService, автоматически вызывается через scheduler после завершенных записей. **Добавлена полная система отзывов:** создана сущность Review с рейтингом (1-5) и модерацией (pending/approved/rejected), модуль Reviews (service, controller). **Реализована интерактивная форма в Telegram боте** для сбора отзывов с выбором рейтинга через inline-кнопки и текстовым комментарием. Автоматическое обновление рейтинга мастера на основе одобренных отзывов. **Добавлена кнопка "Оставить отзыв"** в уведомлениях FEEDBACK_REQUEST через Telegram. **Добавлено отображение отзывов в профиле мастера** (`admin/app/masters/page.tsx`) - компонент MasterReviews показывает последние 3 одобренных отзыва с рейтингом и комментариями.
  - **Улучшения:** Добавить отображение отзывов в профиле услуги. Добавить уведомления мастерам о новых отзывах. Добавить ответы на отзывы. Добавить фильтрацию отзывов по статусу в админ-панели.

- [x] Напоминания о предстоящих записях (настраиваемые интервалы)
  - **Реализовано:** Полностью реализованы напоминания за 24 часа и за 2 часа. **Добавлена настройка интервалов напоминаний в админ-панели** (раздел "Уведомления") с выбором интервалов: 48, 24, 12, 6, 2, 1 час. **Добавлена возможность отключения напоминаний для конкретных клиентов** через поле `notificationSettings.remindersEnabled` в User entity. Scheduler проверяет настройки клиента перед отправкой напоминаний. **Реализовано сохранение настроек интервалов на сервер** через API `/settings/reminder-intervals` (GET/PUT). Глобальные настройки интервалов применяются по умолчанию для всех клиентов, если у клиента нет индивидуальных настроек. Scheduler использует глобальные настройки с сервера через SettingsService. **Исправлена логика проверки интервалов** - добавлена валидация массива интервалов для корректной работы с настройками из базы данных.
  - **Улучшения:** Добавить индивидуальные настройки интервалов для каждого клиента через профиль клиента. Добавить уведомления админу при изменении глобальных настроек. Добавить историю изменений настроек интервалов.

- [x] Уведомления о отменах и переносах
  - **Реализовано:** Есть методы `sendAppointmentCancellation` и `sendAppointmentRescheduled` в NotificationsService. Уведомления отправляются автоматически при отмене/переносе записи.
  - **Улучшения:** Добавить выбор причины отмены/переноса. Добавить возможность отключения уведомлений для конкретных случаев.

---

## 5. Telegram бот

### Приоритет: Средний

- [ ] Интеграция с платежными системами (оплата через бот)
  - **Не реализовано:** Нет интеграции с платежными системами в боте.
  - **Улучшения:** Интегрировать Telegram Payments API. Добавить оплату через ЮKassa/Stripe через бот.

- [x] Запись через бот с выбором времени
  - **Реализовано:** Полностью реализован процесс записи через Telegram бот (`backend/src/modules/telegram/telegram-bot.service.ts`). Есть выбор услуги, мастера, даты и времени. Есть команда `/start` с кнопкой записи. **Реализована система подкатегорий услуг** - при выборе основной услуги автоматически показываются подкатегории (если есть), с поддержкой множественного выбора подкатегорий (если разрешено в настройках услуги). При выборе нескольких подкатегорий создается несколько записей с автоматическим расчетом общей стоимости и длительности. **Добавлен предпросмотр записи перед подтверждением** - в методе `handleTimeSelect` показывается полная информация об услугах (основная + подкатегории), мастере, дате/времени, общей стоимости и длительности перед финальным подтверждением. **Убран множественный выбор основных услуг** - теперь можно выбрать только одну основную услугу, затем выбрать подкатегории (если есть).
  - **Улучшения:** Улучшить UX процесса записи. Добавить возможность выбора разных мастеров для разных услуг. Добавить визуализацию выбранных подкатегорий в предпросмотре.

- [x] Просмотр истории записей
  - **Реализовано:** Есть команда для просмотра записей в боте. В коде есть обработка `start=appointments`.
  - **Улучшения:** Добавить фильтры по статусу. Добавить детальную информацию о записях. Добавить возможность отмены через бот.

- [x] Отмена/перенос записи через бот
  - **Реализовано:** Полностью реализован функционал переноса записей в боте с выбором новой даты и времени через календарь. **Добавлен выбор причины переноса** (изменение планов, болезнь, срочные дела, другая причина с текстовым вводом). Причина сохраняется при переносе записи. Есть обработка отмены записей через бот (`handleCancelAppointment`). Команды `/cancel` и `/reschedule` доступны в боте.
  - **Улучшения:** Добавить выбор причины при отмене записи (сейчас используется фиксированная причина "Отменено через бота"). Добавить подтверждение отмены с выбором причины. Добавить уведомление мастера об отмене/переносе.

- [x] Система отзывов через бот
  - **Реализовано:** Полностью реализована интерактивная форма для отзывов в Telegram боте. После запроса отзыва через уведомление клиент может выбрать рейтинг (1-5 звезд) через inline-кнопки, затем написать комментарий (или пропустить). Отзыв сохраняется со статусом "pending" и требует модерации админом. После одобрения автоматически обновляется рейтинг мастера. **Добавлена кнопка "💬 Оставить отзыв"** в уведомлениях FEEDBACK_REQUEST, которая автоматически инициирует процесс оставления отзыва. **Добавлено отображение отзывов в профиле мастера** в админ-панели - компонент MasterReviews показывает последние 3 одобренных отзыва с рейтингом и комментариями.
  - **Улучшения:** Добавить возможность редактирования отзывов. Добавить ответы мастеров на отзывы. Добавить отображение отзывов в профиле мастера в боте. Добавить уведомления мастерам о новых отзывах.

- [x] Просмотр баланса бонусов
  - **Реализовано:** Полностью реализован функционал просмотра бонусов. Баланс бонусов отображается в профиле пользователя (`/profile`). **Добавлена отдельная команда `/bonus`** для просмотра баланса с детальной информацией: текущий баланс, статистика (всего начислено, использовано, доступно), последние 5 операций. **Добавлена полная история бонусов** через кнопку "История бонусов" в профиле - показывает все операции с бонусами (начисление, использование, возврат) с датами и временем, группировкой по датам. Используется `FinancialService.getUserTransactions()` для получения транзакций. История показывает до 20 последних операций с детальной информацией.
  - **Улучшения:** Добавить фильтрацию истории по типу операций (только начисления, только использования). Добавить экспорт истории в PDF/Excel. Добавить график накопления бонусов.

- [ ] Получение чеков и документов
  - **Не реализовано:** Нет функционала чеков в боте.
  - **Улучшения:** Добавить генерацию чеков в PDF. Добавить отправку чеков через бот. Интегрировать с ФНС (54-ФЗ).

- [ ] Многоязычность бота
  - **Не реализовано:** Бот только на русском языке.
  - **Улучшения:** Добавить поддержку i18n. Добавить выбор языка в настройках. Добавить переводы интерфейса.

- [x] Работа бота в группах и чатах
  - **Частично реализовано:** Базовая поддержка работы в группах реализована. Есть проверка типа чата (`isGroupChat`, `ctx.chat.type`), обработка добавления бота в группу (`setupGroupHandlers`), обработка упоминаний бота (`setupMentionHandler`). В группах показывается упрощенное меню с кнопками для перехода в личный чат. Приватные действия перенаправляются в личные сообщения через `sendPrivateReply`. Есть приветственные сообщения для новых участников группы. Сохранение информации о чатах в БД (`saveChatInfo`, `TelegramChat` entity).
  - **Что реализовано:**
    - ✅ Обработка типов чатов (`private`, `group`, `supergroup`, `channel`) - метод `isGroupChat`, enum `ChatType` в `TelegramChat` entity
    - ✅ Команда `/start` в группах - показывает упрощенное меню с inline-кнопками и перенаправлением в личный чат
    - ✅ Обработка упоминаний бота (`@botname`) - middleware `setupMentionHandler` с проверкой entities
    - ✅ Приватные ответы в группах - метод `sendPrivateReply` отправляет ответы через личные сообщения, с fallback на `reply_to_message_id` в группе
    - ✅ Приветственные сообщения новым участникам группы - метод `sendWelcomeMessageToNewMember`, обработчик `new_chat_members`
    - ✅ Сохранение информации о чатах в БД - метод `saveChatInfo`, entity `TelegramChat` с полями: `chatId`, `type`, `title`, `username`, `description`, `photoUrl`, `membersCount`, `isActive`, `metadata`
    - ✅ Блокировка приватных команд в группах - проверка `isGroupChat` перед выполнением команд (`/book`, `/appointments`, `/services`, `/profile`)
    - ✅ Middleware для обработки упоминаний - `bot.use()` middleware в `setupMentionHandler`
    - ✅ Использование `ctx.chat.type` - проверяется в `isGroupChat` и `setupMentionHandler`
    - ✅ Использование `ctx.from.id` - для идентификации пользователя в группе
    - ✅ Обработка событий группы - `new_chat_members`, `left_chat_member`, `new_chat_title`, `new_chat_photo`, `delete_chat_photo`, `pinned_message`
    - ✅ Проверка прав админа системы - метод `isAdmin` (проверяет роль в БД, не права администратора группы)
  - **Что реализовано (обновлено):**
    - ✅ Публичные команды для групп (`/schedule`, `/masters`, `/promotions`, `/faq`) - реализованы методы `handleScheduleCommand`, `handleMastersCommand`, `handlePromotionsCommand`, `handleFaqCommand`. Команды работают в группах и показывают публичную информацию без перенаправления в личный чат. Есть проверка включения команд через настройки группы.
    - ✅ Inline-режим для быстрого поиска - реализован обработчик `setupInlineQuery` с методом `bot.on('inline_query')` и `ctx.answerInlineQuery()`. Поддерживает поиск услуг и просмотр записей через inline-запросы.
    - ✅ Настройки группы - создана entity `GroupSettings` с полями: `chatId`, `language`, `enabledCommands` (schedule, masters, promotions, faq), `notifications` (welcomeEnabled, newMemberEnabled). Создана миграция `009-create-group-settings.ts`. Реализованы методы `getGroupSettings()` и `isCommandEnabled()` для управления настройками.
    - ✅ Приватные кнопки с `switch_inline_query` - обновлены кнопки в `/start` для групп и приветственных сообщениях. Используется `Markup.button.switchInlineQuery()` для кнопок "Записаться" и "Мои записи".
  - **Что не реализовано:**
    - ❌ Модерация в группах (команды для администраторов группы) - нет проверки `getChatAdministrators`, нет команд `/ban_client`, `/warn_client`, `/stats_group`
    - ❌ Статистика группы (`/stats` для группы) - команда `/stats` есть только для админов системы, показывает общую статистику бота
    - ❌ Rate limiting для команд в группах - нет механизма ограничения частоты запросов
    - ❌ Обработка команд с упоминанием (`@botname /command`) - упоминания обрабатываются, но команды с упоминанием не парсятся отдельно
    - ❌ Обработка reply на сообщения бота - нет специальной обработки `ctx.message.reply_to_message` (кроме fallback в `sendPrivateReply`)
    - ❌ Проверка прав администратора группы - нет использования `getChatAdministrators` API
  - **Рекомендации по доработке:**
    - **Модерация в группах:** Добавить команды для администраторов группы (`/ban_client`, `/warn_client`, `/stats_group`). Использовать `ctx.telegram.getChatAdministrators(ctx.chat.id)` для проверки прав.
    - **Обработка команд с упоминанием:** Добавить парсинг команд вида `@botname /command` в middleware или обработчике команд.
    - **Обработка reply:** Добавить обработку `ctx.message.reply_to_message` для ответов на сообщения бота в группах.
    - **Rate limiting:** Реализовать механизм ограничения частоты запросов (например, через Map с временными метками или библиотеку `telegraf-throttler`).
    - **Статистика группы:** Добавить команду `/stats_group` для показа статистики записей, созданных участниками конкретной группы.
    - **Команда /settings_group:** Добавить команду для настройки поведения бота в группе (включить/выключить команды, изменить язык, настроить уведомления).
    - **Улучшение /schedule:** Использовать реальную логику из `AppointmentsService.getAvailableSlots()` для более точного расчета свободных слотов.
  - **Технические детали реализации:**
    - ✅ Используется `ctx.chat.type` для определения типа чата (строка 572, 143, 2863)
    - ✅ Используется `ctx.from.id` для идентификации пользователя в группе (строка 574, 175, 2885)
    - ✅ Есть middleware для обработки упоминаний (`bot.use()` в `setupMentionHandler`, строка 130)
    - ✅ Есть отдельная логика для групповых и приватных команд (проверка `isGroupChat` в командах)
    - ✅ Есть entity `TelegramChat` для хранения информации о чатах
    - ✅ Создана entity `GroupSettings` для хранения настроек групп (`backend/src/entities/group-settings.entity.ts`)
    - ✅ Создана миграция `009-create-group-settings.ts` для таблицы `group_settings`
    - ✅ Реализованы публичные команды: `handleScheduleCommand()`, `handleMastersCommand()`, `handlePromotionsCommand()`, `handleFaqCommand()`
    - ✅ Реализован inline-режим: метод `setupInlineQuery()` с обработкой `bot.on('inline_query')` и `ctx.answerInlineQuery()`
    - ✅ Используется `switch_inline_query` в кнопках: `Markup.button.switchInlineQuery()` для кнопок в группах
    - ✅ Методы для работы с настройками: `getGroupSettings()`, `isCommandEnabled()` для проверки включенных команд

---

## 6. Админ-панель

### Приоритет: Средний

- [x] Дашборд с виджетами (настраиваемый)
  - **Реализовано:** Есть дашборд (`admin/app/dashboard/page.tsx`) с графиками, статистикой, карточками метрик. Используется AnalyticsService для получения данных. Показывает статистику по записям, доходам, мастерам, графикам по дням.
  - **Улучшения:** Добавить возможность настройки виджетов (перетаскивание, скрытие). Добавить больше типов виджетов. Добавить сохранение настроек дашборда.

- [x] Экспорт данных в различные форматы
  - **Реализовано:** Есть компонент ExportButton (`admin/app/components/ExportButton.tsx`) с поддержкой CSV, Excel, PDF. Используется на нескольких страницах.
  - **Улучшения:** Добавить экспорт для всех страниц. Улучшить форматирование экспортируемых данных. Добавить настройки экспорта.

- [ ] Импорт клиентов из Excel/CSV
  - **Не реализовано:** Нет функционала импорта.
  - **Улучшения:** Добавить загрузку файлов. Добавить валидацию данных. Добавить предпросмотр перед импортом.

- [x] Массовые операции (рассылки, изменения статусов)
  - **Реализовано:** Есть массовая рассылка через NotificationsController (`/notifications/broadcast`). Есть массовое удаление уведомлений.
  - **Улучшения:** Добавить массовое изменение статусов записей. Добавить массовую отправку сообщений клиентам. Добавить выбор получателей через фильтры.

- [x] Система ролей и прав доступа
  - **Реализовано:** Есть система ролей (UserRole enum: ADMIN, CLIENT). Есть RolesGuard и декоратор @Roles. Используется для ограничения доступа к эндпоинтам. Есть JWT аутентификация с refresh токенами, логирование авторизации (AuthLog entity), управление сессиями (Session entity).
  - **Улучшения:** Добавить больше ролей (менеджер, мастер). Добавить детальные права доступа (CRUD для разных сущностей). Добавить UI для управления правами.

- [x] История изменений (кто и что изменил)
  - **Реализовано:** Полностью реализован Audit модуль (`backend/src/modules/audit/`). Есть AuditService, AuditController, AuditLog entity. Логируются все важные действия (создание, изменение, удаление записей, рассылки и т.д.).
  - **Улучшения:** Добавить фильтры в журнале действий. Добавить экспорт журнала. Добавить детализацию изменений (что именно изменилось).

- [ ] Резервное копирование данных
  - **Не реализовано:** Нет автоматического резервного копирования.
  - **Улучшения:** Добавить автоматическое резервное копирование БД. Добавить ручной экспорт данных. Добавить восстановление из бэкапа.

- [x] Rate limiting и защита от злоупотреблений
  - **Реализовано:** Реализован RateLimitMiddleware (`backend/src/middleware/rate-limit.middleware.ts`) с настраиваемыми лимитами. Есть специальные лимиты для аутентификации (5 попыток в 15 минут) и создания записей (10 записей в час). Используется express-rate-limit.
  - **Улучшения:** Добавить настраиваемые лимиты через админ-панель. Добавить whitelist для IP адресов. Добавить уведомления админу при превышении лимитов.

- [x] Кэширование данных
  - **Реализовано:** Реализован CacheService (`backend/src/common/cache/cache.service.ts`) с поддержкой TTL, очистки по паттернам, статистики кэша. Используется в MastersService для кэширования данных мастеров.
  - **Улучшения:** Добавить Redis для распределенного кэширования. Добавить инвалидацию кэша при изменениях. Добавить мониторинг эффективности кэша.

- [x] WebSocket для real-time обновлений
  - **Реализовано:** Реализован WebSocketGateway (`backend/src/modules/websocket/websocket.gateway.ts`) с поддержкой подписки на обновления слотов, уведомлений о новых записях и изменениях статусов. Используется Socket.IO.
  - **Улучшения:** Добавить аутентификацию WebSocket соединений. Добавить комнаты для разных типов событий. Добавить переподключение при разрыве связи.

- [x] Логирование авторизации
  - **Реализовано:** Есть AuthLog entity для логирования всех действий авторизации (login, logout, login_failed, token_refresh). Сохраняется IP адрес, user agent, ошибки. Есть индексы для быстрого поиска.
  - **Улучшения:** Добавить UI для просмотра логов авторизации в админ-панели. Добавить фильтры по пользователю, дате, типу действия. Добавить уведомления о подозрительной активности.

- [x] Управление сессиями
  - **Реализовано:** Есть Session entity для управления активными сессиями пользователей. Хранятся refresh токены, IP адреса, user agents, сроки действия. Поддерживается деактивация сессий.
  - **Улучшения:** Добавить UI для управления сессиями в профиле пользователя. Добавить возможность завершения всех сессий. Добавить уведомления о новых входах.

- [ ] Восстановление удаленных записей
  - **Не реализовано:** Нет soft delete для записей.
  - **Улучшения:** Добавить soft delete (поле `deletedAt`). Добавить корзину удаленных записей. Добавить восстановление из корзины.

---

## 7. Мастера

### Приоритет: Средний

- [ ] Личный кабинет мастера (просмотр своих записей)
  - **Не реализовано:** Нет отдельного личного кабинета для мастеров.
  - **Улучшения:** Создать отдельный раздел для мастеров. Добавить просмотр только своих записей. Добавить возможность изменения статуса записи мастером.

- [ ] Статистика работы мастера
  - **Частично реализовано:** Есть статистика загруженности мастера через `getMasterLoad`.
  - **Улучшения:** Добавить детальную статистику (количество клиентов, доход, средний чек). Добавить графики работы мастера.

- [x] График работы мастера (смены)
  - **Реализовано:** Есть WorkSchedule entity (`backend/src/entities/work-schedule.entity.ts`), используется для управления расписанием мастеров. Есть страница управления расписанием (`admin/app/masters/[id]/schedule/page.tsx`).
  - **Улучшения:** Добавить визуализацию графика работы. Добавить копирование расписания на неделю. Добавить шаблоны расписаний.

- [ ] Система рейтингов мастеров
  - **Не реализовано:** Нет системы рейтингов.
  - **Улучшения:** Добавить сбор рейтингов через отзывы. Добавить расчет среднего рейтинга. Добавить отображение рейтинга в профиле мастера.

- [ ] Комиссии и выплаты мастерам
  - **Не реализовано:** Нет системы комиссий.
  - **Улучшения:** Добавить настройку процента комиссии для каждого мастера. Добавить расчет заработка мастера. Добавить историю выплат.

- [ ] Отпуска и больничные
  - **Частично реализовано:** Есть BlockInterval для блокировки времени, можно использовать для отпусков.
  - **Улучшения:** Добавить специальный тип блокировки (отпуск, больничный). Добавить календарь отпусков. Добавить уведомления о предстоящих отпусках.

- [x] Специализации мастеров (расширенная)
  - **Реализовано:** Есть связь many-to-many между Master и Service. Мастер может иметь несколько услуг.
  - **Улучшения:** Добавить категории специализаций. Добавить уровни мастерства. Добавить сертификаты и достижения.

---

## 8. Услуги и прайс-лист

### Приоритет: Низкий

- [ ] Пакеты услуг (комбо-предложения)
  - **Не реализовано:** Нет функционала пакетов услуг.
  - **Улучшения:** Добавить сущность ServicePackage. Добавить связь many-to-many с услугами. Добавить скидки на пакеты.

- [ ] Сезонные акции и скидки
  - **Не реализовано:** Нет системы акций.
  - **Улучшения:** Добавить таблицу Promotions с датами действия. Добавить автоматическое применение акций.

- [x] Категории услуг с подкатегориями
  - **Реализовано:** Полностью реализована система подкатегорий услуг. Добавлены поля `parentServiceId`, `allowMultipleSubcategories` и `isCategory` в Service entity. Созданы миграции БД для новых полей. Реализована иерархическая структура услуг с поддержкой рекурсивных связей (родительская услуга → подкатегории). **Добавлен полный UI в админ-панели** (`admin/app/services/page.tsx`) для управления подкатегориями: выбор типа услуги (основная/категория/подкатегория), выбор родительской категории при создании подкатегории, настройка множественного выбора подкатегорий, отображение подкатегорий под родительскими услугами. **Обновлена логика Telegram бота** - убран множественный выбор основных услуг, добавлен выбор подкатегорий с поддержкой множественного выбора (если разрешено в настройках). При выборе основной услуги автоматически показываются подкатегории (если есть), иначе сразу переход к выбору мастера. **Обновлено создание записей** - при выборе нескольких подкатегорий создается несколько записей (по одной на каждую подкатегорию) с автоматическим расчетом общей стоимости и длительности. Добавлены методы `findSubcategories()`, `findMainServices()`, `findCategories()` и `findServicesForBot()` в ServicesService. Реализована валидация: `allowMultipleSubcategories` может быть true только для категорий (без `parentServiceId`), категории не могут иметь цену и длительность. **Исправлено отображение комбинированных записей в календаре** - все записи, включая созданные при выборе нескольких подкатегорий, корректно отображаются в календаре админ-панели.
  - **Улучшения:** Добавить визуализацию иерархии услуг в виде дерева. Добавить фильтрацию по категориям. Добавить экспорт услуг с подкатегориями. Добавить возможность изменения порядка подкатегорий.

- [ ] Фото и описание услуг
  - **Частично реализовано:** Есть базовое описание услуги в Service entity.
  - **Улучшения:** Добавить поле для фото услуг. Добавить расширенное описание с форматированием. Добавить галерею фото.

- [ ] Рекомендации услуг на основе истории клиента
  - **Не реализовано:** Нет системы рекомендаций.
  - **Улучшения:** Использовать историю посещений для рекомендаций. Добавить алгоритм рекомендаций. Показывать рекомендации в боте и админ-панели.

---

## 9. Маркетинг

### Приоритет: Низкий

- [x] Программа лояльности (накопительные скидки)
  - **Реализовано:** Есть система бонусов (FinancialService), начисление и использование бонусов. Есть поле `bonusPoints` в User entity.
  - **Улучшения:** Добавить уровни лояльности с разными процентами начисления. Добавить накопительные скидки (чем больше посещений, тем больше скидка).

- [ ] Реферальная программа
  - **Не реализовано:** Нет реферальной программы.
  - **Улучшения:** Добавить поле `referrerId` в User. Добавить начисление бонусов за приглашенных клиентов. Добавить отслеживание рефералов.

- [ ] Промокоды и купоны
  - **Не реализовано:** Нет системы промокодов.
  - **Улучшения:** Добавить таблицу PromoCodes. Добавить генерацию промокодов. Добавить применение промокодов при записи.

- [x] Email/SMS маркетинг
  - **Частично реализовано:** Есть массовая рассылка через NotificationsController (`/notifications/broadcast`). Есть поддержка разных каналов (Telegram, Email, SMS), но Email/SMS только заглушки.
  - **Улучшения:** Реализовать отправку Email/SMS. Добавить сегментацию получателей. Добавить A/B тестирование рассылок.

- [ ] Аналитика эффективности маркетинговых кампаний
  - **Не реализовано:** Нет аналитики маркетинга.
  - **Улучшения:** Добавить отслеживание открытий и кликов. Добавить конверсию из рассылок в записи. Добавить ROI маркетинговых кампаний.

---

## 10. Интеграции

### Приоритет: Низкий

- [ ] Интеграция с онлайн-платежами (ЮKassa, Stripe)
- [ ] Интеграция с CRM системами
- [ ] API для сторонних приложений
- [ ] Webhook для внешних систем
- [ ] Интеграция с социальными сетями

---

## 11. Мобильное приложение

### Приоритет: Низкий

- [ ] Мобильное приложение для клиентов (iOS/Android)
- [ ] Мобильное приложение для мастеров
- [ ] Push-уведомления
- [ ] Офлайн-режим

---

## 12. Frontend (клиентское приложение)

### Приоритет: Средний

- [x] Онлайн-запись через веб-интерфейс
  - **Реализовано:** Полностью реализован процесс записи через веб-интерфейс (`frontend/src/pages/`). Есть страницы: Calendar (календарь для выбора даты), MasterSelection (выбор мастера), ServiceDetail (детали услуги), AppointmentConfirmation (подтверждение записи), Reschedule (перенос записи). Используется Telegram WebApp для авторизации через TelegramContext.
  - **Улучшения:** Добавить визуализацию доступных слотов. Добавить фильтры по услугам и мастерам. Добавить сохранение избранных мастеров.

- [x] Личный кабинет клиента
  - **Реализовано:** Есть страницы Profile (профиль), History (история записей), Notifications (уведомления). Показывается информация о клиенте, баланс бонусов, история записей.
  - **Улучшения:** Добавить редактирование профиля. Добавить настройки уведомлений. Добавить график посещений.

- [x] Каталог услуг
  - **Реализовано:** Есть страница Services с отображением всех услуг через ServiceCard компонент. Есть страница ServiceDetail с детальной информацией об услуге.
  - **Улучшения:** Добавить фильтрацию по категориям. Добавить поиск услуг. Добавить сортировку.

- [x] Обработка ошибок и offline режим
  - **Реализовано:** Есть ErrorBoundary компонент для обработки ошибок React. Есть OfflineIndicator компонент для отображения статуса подключения. Есть useOnlineStatus hook для отслеживания онлайн/офлайн статуса.
  - **Улучшения:** Добавить кэширование данных для offline режима. Добавить синхронизацию при восстановлении связи.

- [x] UI компоненты и стилизация
  - **Реализовано:** Есть базовые UI компоненты (button, card, input) в `frontend/src/components/ui/`. Есть LoadingSpinner, SkeletonLoader, EmptyState компоненты. Используется Tailwind CSS для стилизации.
  - **Улучшения:** Добавить больше UI компонентов. Добавить темную тему. Добавить анимации.

---

## 13. Дополнительные функции

### Приоритет: Низкий

- [x] Система отзывов и рейтингов
  - **Реализовано:** Полностью реализована система отзывов. Создана сущность Review с рейтингом (1-5), текстовым комментарием и модерацией (pending/approved/rejected). Создан модуль Reviews (service, controller) с методами создания, получения списка и модерации отзывов. Реализован сбор отзывов через Telegram бот с интерактивной формой. Автоматическое обновление рейтинга мастера на основе одобренных отзывов. Есть API для модерации отзывов админом. **Добавлено отображение отзывов в профиле мастера** в админ-панели (`admin/app/masters/page.tsx`) - компонент MasterReviews показывает последние 3 одобренных отзыва с визуализацией рейтинга (звезды) и комментариями.
  - **Улучшения:** Добавить отображение отзывов в профиле услуги. Добавить фильтрацию отзывов по статусу, мастеру, услуге в админ-панели. Добавить экспорт отзывов. Добавить ответы мастеров на отзывы. Добавить уведомления мастерам о новых отзывах.

- [ ] Фотогалерея работ
  - **Не реализовано:** Нет галереи.
  - **Улучшения:** Добавить таблицу Gallery с фото работ. Добавить привязку к мастерам и услугам. Добавить загрузку фото через админ-панель.

- [ ] Блог/новости салона
  - **Не реализовано:** Нет блога.
  - **Улучшения:** Добавить сущность BlogPost. Добавить редактор статей. Добавить публикацию новостей в Telegram бот.

- [ ] Онлайн-консультации
  - **Не реализовано:** Нет онлайн-консультаций.
  - **Улучшения:** Интегрировать видеозвонки (Zoom, Jitsi). Добавить запись на консультации. Добавить чат для консультаций.

- [ ] Видео-записи процедур (с согласия клиента)
  - **Не реализовано:** Нет видео-записей.
  - **Улучшения:** Добавить загрузку видео. Добавить согласие клиента. Добавить хранение видео.

- [ ] Система напоминаний о процедурах ухода
  - **Не реализовано:** Нет напоминаний об уходе.
  - **Улучшения:** Добавить настройку напоминаний для каждой услуги. Добавить отправку рекомендаций по уходу после процедуры.

---

## Рекомендации по приоритетам

### Сначала (быстрый эффект)

- [x] История посещений клиента ✅ **Реализовано** - можно улучшить фильтрами и экспортом
- [x] Детальная финансовая отчетность ✅ **Реализовано** - можно улучшить добавлением расходов и прибыли
- [ ] Повторяющиеся записи - **Не реализовано**, высокий приоритет
- [ ] Email-уведомления - **Частично** (есть структура, нужна реализация)
- [x] Система отзывов ✅ **Частично реализовано** - есть запрос отзывов, нужно добавить сбор и отображение

### Затем (средний приоритет)

- [ ] Личный кабинет мастера - **Не реализовано**
- [ ] Интеграция с платежными системами - **Не реализовано**
- [x] Расширенная аналитика ✅ **Реализовано** - можно улучшить дополнительными метриками
- [ ] Система промокодов - **Не реализовано**

### Позже (долгосрочные)

- [ ] Мобильное приложение - **Не реализовано**
- [ ] Интеграции с внешними системами - **Не реализовано**
- [ ] Многоязычность - **Не реализовано**

---

## Статус реализации

- Всего функций: 120+
- Реализовано: ~50
- Частично реализовано: ~10
- В процессе: 0
- Запланировано: ~60

### Дополнительные реализованные функции (не указанные выше):

- ✅ WebSocket для real-time обновлений
- ✅ Кэширование данных (CacheService)
- ✅ Rate limiting и защита от злоупотреблений
- ✅ Логирование авторизации (AuthLog)
- ✅ Управление сессиями (Session entity)
- ✅ Система транзакций (Transaction entity с типами: PAYMENT, BONUS_EARNED, REFUND)
- ✅ Возврат бонусов при отмене записи
- ✅ Настройки timezone для корректного отображения времени
- ✅ Настройки бронирования (manualConfirmation, minAdvanceBooking, maxAdvanceBooking, cancellationDeadline)
- ✅ Настройки скидки для первого визита
- ✅ Analytics API (dashboard stats, master load)
- ✅ Frontend приложение с полным циклом записи
- ✅ Обработка ошибок и offline режим в frontend
- ✅ Интеграция с Telegram WebApp

---

## Примечания

- Приоритеты могут изменяться в зависимости от потребностей бизнеса
- Некоторые функции могут быть объединены или разделены
- Реализация зависит от доступных ресурсов и времени
